<%= react_component(
    'TagPhoto',
    flickrId: @flickr_id,
    url: @photo_url,
    userTags: @user_tags,
    farm: @farm,
    server: @server,
    secret: @secret,
    currentRoute: request.path
  ) %>
